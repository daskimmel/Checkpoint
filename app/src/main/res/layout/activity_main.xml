<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/primary"
        tools:context=".MainActivity"
        android:windowSoftInputMode="adjustPan">

    <TextView
            android:id="@+id/message"
            android:layout_width="267dp"
            android:layout_height="0dp"
            android:text="@string/title_home"
            app:layout_constraintTop_toTopOf="parent"
            android:textSize="36sp"
            android:fontFamily="@font/pacifico" android:textColor="@color/accent" android:gravity="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

    <android.support.design.widget.BottomNavigationView
            android:id="@+id/navigation"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/secondary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:menu="@menu/navigation" app:itemTextColor="@color/black" app:itemBackground="@color/accent"/>
    <FrameLayout
            android:layout_width="416dp"
            android:layout_height="503dp" app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" app:layout_constraintHorizontal_bias="0.6"
            android:id="@+id/main_fragment" app:layout_constraintVertical_bias="0.706">

    </FrameLayout>

</android.support.constraint.ConstraintLayout>